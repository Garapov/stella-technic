<div class="block p-6 bg-white border border-gray-200 rounded-lg shadow-sm hover:bg-gray-100 dark:bg-gray-800 dark:border-gray-700 dark:hover:bg-gray-700" data-fancybox="{{ $selector }}" data-caption="{{ $certificate->name }}" data-src="{{ Storage::disk(config('filesystems.default'))->url($certificate->image) }}" title="{{ $certificate->name }}">
    <img class="w-full h-full object-cover object-center rounded-lg" src="{{ Storage::disk(config('filesystems.default'))->url($certificate->image) }}" alt="{{ $certificate->name }}">
</div>